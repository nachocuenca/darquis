services:
  darquis:
    container_name: darquis-web
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:13080:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
